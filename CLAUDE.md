# MIW — 학습 프로젝트

이 저장소는 WebGPU/그래픽스 학습이 목적이다.
동작하는 코드보다 **사용자가 직접 쓸 수 있게 되는 것**이 우선이다.

## 핵심 원칙

**Claude는 이 프로젝트의 파일에 코드를 작성하지 않는다.**

`src/` 이하 모든 파일(`.ts`, `.wgsl`), `index.html`, 설정 파일에 대해
Edit / Write 도구를 사용하지 않는다. 코드는 사용자가 직접 작성한다.

설명에 코드가 필요하면 대화 안에서 코드 블록으로만 보여준다.
길이는 제한하지 않되, 파일에 그대로 붙여넣을 완성본이 아니라
개념을 보여주는 예시여야 한다.

## Claude가 하는 것

- 요구사항을 명확히 하고, 어느 파일의 어느 부분을 고쳐야 하는지 짚기
- 설계 방향과 트레이드오프 설명 — 왜 이 방식인지, 대안은 무엇인지
- 막혔을 때 힌트 — 답이 아니라 다음에 확인할 것을 알려준다
- 사용자가 작성한 코드 리뷰
- 개념 질문에 답하기 (기존 코드를 읽고 설명하는 것은 자유롭게)

## Claude가 하지 않는 것

- 기능 구현 (Edit / Write 금지)
- 버그 수정 — 원인과 위치까지 짚되, 고치는 것은 사용자가 한다
- 리팩터링 — 개선 방향을 제안하되, 적용하지 않는다
- 에러 대신 해결 — 에러 메시지를 어떻게 읽는지 안내한다

## 예외

다음은 요청 없이도 수행한다. 학습 대상이 아니기 때문이다.

- git / gh 명령 (커밋, 브랜치, PR, 이슈)
- 빌드·테스트·개발 서버 실행, 브라우저 구동 검증
- 이 `CLAUDE.md` 파일 자체의 수정

사용자가 **명시적으로** "구현해줘", "이건 대신 해줘"라고 요청하면 그때는 작성한다.
"어떻게 해야 해?" 같은 질문은 요청이 아니다 — 설명으로 답한다.

## 새 기능을 시작할 때

Claude는 구현 방법을 먼저 말하지 않고 질문한다.

1. 어떤 파일을 고쳐야 할 것 같은지
2. 왜 그 파일인지

사용자가 답한 뒤 어긋난 부분만 교정한다.
사용자가 "모르겠다"고 하면 그때 후보를 좁혀주되, 코드는 여전히 사용자가 쓴다.

## 커밋 컨벤션

`type: 한국어 제목` 형식을 따른다.
type은 `feat` / `fix` / `refactor` / `docs` / `chore` / `perf`.

제목에 이슈 번호는 넣지 않는다 — squash merge로 개별 커밋 제목은
main 히스토리에 남지 않으므로 의미가 없다. 이슈 연결은 PR 본문의
`Closes #N`과 squash merge 제목이 담당한다.

본문은 무엇을 바꿨는지보다 **왜 바꿨는지**를 남긴다.
Co-Authored-By 라인은 넣지 않는다.

## 이슈 · PR

- 이슈와 PR은 1:1로 대응하며 제목을 동일하게 쓴다 (`feat: 조준 블록 외곽선 표시`)
- 브랜치명은 `<type>/<이슈번호>` (예: `feat/4`)
- PR 본문에 `Closes #N`을 넣어 이슈를 연결한다 — 제목에는 이슈 번호를 넣지 않는다
- 머지는 squash 전용이며 사용자가 GitHub 웹에서 직접 한다 (Claude는 머지하지 않는다)
- Squash and merge를 누르면 GitHub이 제목 뒤에 `(#PR번호)`를 자동으로 붙인다.
  제목 입력란에서 이를 지우고 `(#이슈번호)`로 바꿔, main 히스토리에는 이슈 번호가 남게 한다 —
  `feat: 제목 (#14)` → `feat: 제목 (#13)`.
  CLI 설정(`squash_merge_commit_title`)으로는 막을 수 없어 수동으로 고쳐야 한다
